<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.RabbiHossainLTD" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <!--
        Splash/Launcher theme: fullscreen, no title bar.
        LauncherActivity uses this so there's no flash of UI before Chrome opens.
    -->
    <style name="Theme.RabbiHossainLTD.Splash" parent="Theme.RabbiHossainLTD">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

</resources>
